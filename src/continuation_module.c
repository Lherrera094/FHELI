#include "continuation_module.h"

void control_continuity( saveData *saveDCfg ){
    //Function checks if continuation is possible and proceed to save the fields for continuation



}

void check_hdf5( saveData *saveDCfg ){

    char fullDir[PATH_MAX], filename_hdf5[PATH_MAX];
    sprintf(fullDir,"%s/%s/", projectPath, foldername);

    //Function checks if continuation is possible and proceed to save the fields for continuation

}